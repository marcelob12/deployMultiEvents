import React from "react";

const Locality = ({ loc }) => {
    return (
            <button className="p-2 text-center w-full font-Saira font-bold hover:cursor-pointer">
                {loc}
            </button>
    )
}

export default Locality